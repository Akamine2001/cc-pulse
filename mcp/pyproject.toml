[project]
name = "cc-pulse-mcp"
version = "0.1.0"
description = "MCP Server for article similarity search with EmbeddingGemma"
requires-python = ">=3.10"

dependencies = [
  "huggingface-hub>=0.20.0",
  "optimum[onnxruntime]>=1.16.0",
  "transformers>=4.36.0",
  "numpy>=1.24.0",
  "mcp>=0.1.0",
]
