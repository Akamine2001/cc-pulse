レビュー・テスト観点を作成しました

実装時は以下のIssueを参照してください：
- #{{SUB_ISSUE_NUMBER}}

---

**次のステップ:**

サブIssueの内容を確認後、このIssueまたはサブIssueに以下のコマンドをコメントすると、自動実装が開始されます。

基本的な使い方:
`/jules`

ブランチを指定する場合:
`/jules -b feature-v2`
`/jules --branch hotfix/critical-fix`

サブIssueなしで強制的にセッションを作成する場合:
`/jules -f`
`/jules --force`
