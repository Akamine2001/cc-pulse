[project]
name = "pr-review-duplicate-checker"
version = "0.1.0"
description = "MCP Server for duplicate review issue detection with embedding similarity"
requires-python = ">=3.10"

dependencies = [
  "huggingface-hub>=0.20.0",
  "optimum[onnxruntime]>=1.16.0",
  "transformers>=4.36.0",
  "numpy>=1.24.0",
  "mcp>=0.1.0",
  "pydantic>=2.0.0",
]
